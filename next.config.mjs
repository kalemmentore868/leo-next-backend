/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['randomuser.me', 'loremflickr.com', 'firebasestorage.googleapis.com'], 
      },
};

export default nextConfig;
